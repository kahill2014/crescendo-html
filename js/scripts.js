new Splide( '#events-carousel', {
	perPage    : 3,
	perMove    : 1,
    autoWidth  : true,
	autoHeight : true,
    arrows     : false,
	focus      : 'center',
	trimSpace  : false,
	breakpoints: {
		600: {
			
		}
	}
} ).mount( {  } );